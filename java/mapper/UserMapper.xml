<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 실 백엔드 레포에는 retrieveUserInfoByKey 가 든 UserMapper.xml 이 이미 있다. 이 파일은 아래 구문만 병합한다 -->
<mapper namespace="aoms.pm.cast.mapper.UserMapper">
    <select id="retrieveRoleIdList" parameterType="string" resultType="string">
        /* UserMapper.retrieveRoleIdList */
        SELECT A.ROLE_ID
        FROM CAOWN.TN_CA_USER_ROLE A
            INNER JOIN CAOWN.TN_CA_ROLE B ON A.ROLE_ID = B.ROLE_ID
        WHERE A.USER_ID = #{userId}
          AND B.TASK_ID = 'PM'
          AND B.USE_YN = 'Y'
        ORDER BY A.ROLE_ID
    </select>
</mapper>
